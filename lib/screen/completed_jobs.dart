import 'package:flutter/material.dart';

class CompletedJobs extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text('Completed Page')),);
  }
}
